export declare const FileType: {
    cad: string;
    excel: string;
    imageJPEG: string;
    imagePNG: string;
    image: string;
    octetStream: string;
    pdf: string;
    word: string;
    csv: string;
    zip: string;
};
/**
 * Sanitizes the file name
 * @param fileName
 * @returns
 */
export declare function sanitizeFileName(fileName: string, separator: string): string;
/**
 * Converts base64 to file
 * @param dataUrl
 * @param filename
 * @returns {File}
 */
export declare function dataURLtoFile(dataUrl: any, filename: string): File;
